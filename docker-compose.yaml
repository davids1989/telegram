version: '3'
services:
  api:
    build: .
    command: "node teletriade.js"
    environment:
      - NODE_ENV=production
    expose:
      - "3002"
    ports:
      - "3002:3002"
    networks:
      - rede-triade
    restart: always

  bot:
    build: .
    command: "python teletriade.py"
    depends_on:
      api:
        condition: service_healthy
    networks:
      - rede-triade
    restart: always

networks:
  rede-triade:
    external: true
    name: rede-triade
